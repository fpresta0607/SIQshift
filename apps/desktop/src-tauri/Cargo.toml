[package]
name = "siqshift-desktop"
version = "0.0.0"
edition = "2021"
# 1.89 for `File::try_lock`, the spool's interprocess lock.
rust-version = "1.89"
publish = false
# Two bin targets exist (the app and siqshift-hook); name the app explicitly.
default-run = "siqshift-desktop"

[lib]
name = "siqshift_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

# The hook agent CLIs fire; deliberately separate from the Tauri app binary so
# it starts fast and never touches the webview stack.
[[bin]]
name = "siqshift-hook"
path = "src/bin/siqshift-hook.rs"

# The native-messaging host the browser extension connects to; same separation
# from the app binary as the hook, because the browser launches it directly.
[[bin]]
name = "siqshift-browser-host"
path = "src/bin/siqshift-browser-host.rs"


[build-dependencies]
tauri-build = { version = "2", features = [] }
serde_json = "1"
base64 = "0.22"
minisign = "0.9"

[dev-dependencies]
base64 = "0.22"
minisign = "0.9"
# The upload tests stand a loopback HTTP stub in front of `ApiClient`, which is
# the only way to exercise spool -> wire -> truncate without a live API.
tokio = { version = "1", features = ["net", "io-util", "rt-multi-thread"] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = "0.4.45"
reqwest = { version = "0.12", features = ["json", "rustls-tls"], default-features = false }
tokio = { version = "1", features = ["sync", "macros", "rt", "time", "process", "io-util"] }
keyring = { version = "3", features = ["windows-native", "apple-native", "sync-secret-service"] }
# Activity-segment clientIds and the per-install device id (upload idempotency).
uuid = { version = "1", features = ["v4"] }
tauri-plugin-updater = "2"
# The app lives in the tray: register with the OS login (autostart) and route
# a second launch to the running instance instead of a duplicate monitor.
tauri-plugin-autostart = "2"
tauri-plugin-single-instance = "2"

# The activity monitor's read-only Win32 calls: GetLastInputInfo,
# GetForegroundWindow process names, and session lock/suspend events. The
# Gdi/Shell/Com/FileSystem features exist for `app_icons` icon extraction;
# Shell also covers SHQueryUserNotificationState and Registry the
# ConsentStore microphone read, two of the false-idle rescue probes.
[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61", features = [
    "Win32_Foundation",
    "Win32_Graphics_Gdi",
    "Win32_Storage_FileSystem",
    "Win32_System_Com",
    "Win32_System_Diagnostics_ToolHelp",
    "Win32_System_LibraryLoader",
    "Win32_System_Registry",
    "Win32_System_RemoteDesktop",
    "Win32_System_SystemInformation",
    "Win32_System_Threading",
    "Win32_UI_Accessibility",
    "Win32_UI_Input_KeyboardAndMouse",
    "Win32_UI_Shell",
    "Win32_UI_WindowsAndMessaging",
] }
# The media-playing rescue probe is WinRT (GlobalSystemMediaTransportControls),
# which windows-sys does not carry; already in the tree through Tauri.
windows = { version = "0.61", features = [
    "Foundation",
    "Media_Control",
    "Win32_System_Com",
] }
# `app_icons` PNG-encodes extracted icons into data URIs; both crates already
# sit in the dependency tree (tauri's tray-icon links `png`, reqwest `base64`).
png = "0.18"
base64 = "0.22"

[profile.release]
strip = true
lto = true
